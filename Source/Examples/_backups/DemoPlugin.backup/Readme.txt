This example shows how to create custom GUI extension:
1) with own buttons\menus
2) with own Panel and with own GUI control
3) shows how to persist a custom setting when the project is being saved or opened.